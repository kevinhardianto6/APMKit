# Session — 2026-08-27 — feat-007

Kevin Hardianto. Implemented, tested, and got approval for feat-007 (Breadcrumbs). User said
docs/02 was updated on their side (MOB-12 split into automatic vs. host-invoked sources,
no-swizzling rationale, Android parity note) — checked the repo directly and that update
isn't actually present in `docs/02-Mobile-SDK.md` (MOB-12's line is unchanged, no matches for
"swizzl"/"ActivityLifecycleCallbacks"/"host-invoked" anywhere in docs/). Flagged back to the
user rather than assuming it landed.

## Changes

| File | Change | Why |
|------|--------|-----|
| `archive/features/feat-007.md` | Added | Rotated feat-007 detail on closing it |
| `FEATURES.md` | feat-007 → ✅ (archived); feat-008 → 🔵 in progress | User approved feat-007 |
| (fixed) `FEATURES.md` duplicate lines 114-118 | Removed | Leftover artifact from an earlier Edit call that partially matched; caught before committing |

## What went wrong / worth remembering

- An earlier `Edit` call on `FEATURES.md` left duplicate leftover content (a stray
  "Requirements"/"Done when"/"Decisions — none yet" block) instead of cleanly replacing the
  old feat-007 stub. Caught it by re-reading the file before rotating rather than trusting the
  edit succeeded as intended — worth doing a full re-read after any edit to a file with this
  much structural repetition (multiple `### feat-NNN` sections that look similar), not just
  after complex multi-line replacements.
- The user reported a doc update that turned out not to be in the repo. Always verify a
  claimed external change against the actual file (`grep`/`git diff`) before treating it as
  fact in a decision record, the same discipline already applied to code review.

Evidence: `archive/features/feat-007.md`.
