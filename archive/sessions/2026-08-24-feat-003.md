# Session — 2026-08-24 — feat-003

Kevin Hardianto. Implemented, tested, and committed feat-003 (Network Capture) as `e8abf08`
(originally `0a6be2b`, amended to `b4eb383` to drop a Co-Authored-By trailer, then the whole
feat-001..003 chain was reconstructed via `git commit-tree` to drop it from feat-001/002 too
— `5b3d58c`→`fce4dcf`, `43ef425`→`adfab40`, `b4eb383`→`e8abf08`; trees byte-identical, only
messages changed, verified with `git diff` before repointing `main`. No remote configured.

## Changes

| File | Change | Why |
|------|--------|-----|
| `Sources/APMKit/Network/*.swift` (5 files) | Added | feat-003: FailureCategory(+Mapper), EventSink protocol, NetworkCaptureForwardingDelegate, NetworkCaptureDelegate |
| `Sources/APMKit/APMKit.swift` | Added `APM.instrumentedSession(...)` | feat-003 done-when requirement |
| `Package.swift` | Added `.macOS(.v11)` platform (non-distribution) | Host `swift build`/test needs iOS-15-era API availability |
| `Tests/APMKitTests/Network/*.swift`, `Tests/APMKitTests/Support/{MockHTTPServer,CollectingEventSink}.swift` | Added | 20 tests |
| `docs/01-Kontrak-Data-API.md`, `docs/02-Mobile-SDK.md` | User edited directly (§4.2 status_code, MOB-02b) | Formalized the http_error decision already implemented |
| `FEATURES.md` | feat-003 → ✅ (archived); feat-004 → 🔵 in progress | Evidence recorded |

## What went wrong / worth remembering

- `URLSession.data(from:)` (async convenience) didn't reliably trigger a custom session
  delegate's task callbacks in this test environment — switched all networking tests to
  `dataTask(with:).resume()` + polling. Not fully root-caused; worth retesting if a future
  Swift toolchain update lands.
- `swift test` on macOS needed `.macOS(.v11)` added to `Package.swift` platforms (not a
  distribution target) because `Network`'s `tls_protocol_version_t` requires macOS 10.15+
  availability and the package had no macOS floor declared at all. Third occurrence of the
  same host-vs-iOS-toolchain compile gap (after feat-001's DeviceInfo, feat-002's
  FileProtectionType) — worth checking proactively before starting feat-007 (UIKit lifecycle)
  and feat-008 (simulator/jailbreak checks), which will likely hit it again.
- User hand-edits to spec docs mid-implementation are a real signal to re-diff against, not
  just re-read — the http_error fix required both a doc read AND a matching code change
  (removing synthesized error_domain/error_code) to stay exactly in sync.

Evidence: `archive/features/feat-003.md`.
